def sum(a,b)
{
echo "the sum of ${a} and ${b} is ${a+b}"
}
def sub(a,b)
{
echo "the subtraction result of ${a} and ${b} is ${a-b}"
}
def pro(a,b)
{
echo "the product result of ${a} and ${b} is ${a*b}"
}
def dev(a,b)
{
echo "the devision result of ${a} and ${b} is ${a/b}"
}
